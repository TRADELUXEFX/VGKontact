<?xml version="1.0" encoding="utf-8"?>
<!-- Thin top-only divider line above the limit breakdown rows
     (Free plan / Referral bonus) inside the dashboard's limitBlock card. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:height="1dp" android:gravity="top">
        <shape android:shape="rectangle">
            <solid android:color="@color/stats_card_border" />
        </shape>
    </item>
</layer-list>
