<?xml version="1.0" encoding="utf-8"?>
<!--
    Background for the last section body in the group box (currently
    "All Kontact Groups"). Only the bottom corners are rounded so the
    fill matches the outer rect_box_background's rounded bottom edge
    instead of showing a square cut-off against it.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/stats_card_fill" />
    <corners
        android:bottomLeftRadius="16dp"
        android:bottomRightRadius="16dp"
        android:topLeftRadius="0dp"
        android:topRightRadius="0dp" />
</shape>
