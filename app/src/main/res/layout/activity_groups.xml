<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/stats_card_fill"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/header_gradient"
            android:paddingTop="56dp"
            android:paddingBottom="36dp"
            android:paddingStart="24dp"
            android:paddingEnd="24dp"
            android:gravity="center">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/title_groups"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="@color/white" />

        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!-- Single card matching the Profile screen exactly: one
                 card_background, uppercase muted labels, 1dp divider
                 lines between sections, no nested pill boxes or mixed
                 corner radii. -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="@drawable/card_background"
                android:padding="20dp">

                <!-- Totals label, same style as Profile's field labels -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="KONTACT GROUPS"
                    android:textSize="11sp"
                    android:textAllCaps="true"
                    android:letterSpacing="0.05"
                    android:textColor="@color/text_muted" />

                <TextView
                    android:id="@+id/groupsCountText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textSize="17sp"
                    android:textStyle="bold"
                    android:textColor="@color/vg_dark"
                    android:paddingTop="2dp"
                    android:paddingBottom="16dp"
                    tools:text="10 groups \u00B7 1 joined" />

                <View
                    android:layout_width="match_parent"
                    android:layout_height="1dp"
                    android:background="@color/stats_card_border"
                    android:layout_marginBottom="16dp" />

                <!-- Search - plain boxed field, same visual weight as the
                     referral-code box on Profile, not a pill. -->
                <EditText
                    android:id="@+id/groupSearchInput"
                    android:layout_width="match_parent"
                    android:layout_height="46dp"
                    android:hint="Search groups by ID..."
                    android:inputType="number"
                    android:paddingStart="14dp"
                    android:paddingEnd="14dp"
                    android:background="@drawable/referral_code_background"
                    android:textSize="13sp"
                    android:textColor="@color/vg_dark"
                    android:textColorHint="@color/text_muted"
                    android:fontFamily="@font/manrope"
                    android:imeOptions="actionDone"
                    android:layout_marginBottom="16dp" />

                <!-- Default view: two sections, each a plain label row
                     (tap to expand/collapse) followed by its rows,
                     separated by divider lines - same rhythm as the rest
                     of the Profile card. -->
                <LinearLayout
                    android:id="@+id/sectionsView"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <!-- Your Groups -->
                    <LinearLayout
                        android:id="@+id/yourGroupsHeader"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center_vertical"
                        android:foreground="?attr/selectableItemBackground"
                        android:clickable="true"
                        android:focusable="true">

                        <ImageView
                            android:layout_width="14dp"
                            android:layout_height="14dp"
                            android:src="@drawable/ic_sync"
                            app:tint="@color/vg_green" />

                        <TextView
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_weight="1"
                            android:layout_marginStart="8dp"
                            android:text="YOUR GROUPS"
                            android:textSize="11sp"
                            android:textAllCaps="true"
                            android:letterSpacing="0.05"
                            android:textColor="@color/text_muted" />

                        <ImageView
                            android:id="@+id/yourGroupsChevron"
                            android:layout_width="13dp"
                            android:layout_height="13dp"
                            android:src="@drawable/ic_chevron_down"
                            app:tint="@color/text_muted" />

                    </LinearLayout>

                    <TextView
                        android:id="@+id/yourGroupsSubText"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:textSize="17sp"
                        android:textStyle="bold"
                        android:textColor="@color/vg_dark"
                        android:paddingTop="2dp"
                        android:paddingBottom="12dp"
                        tools:text="1 joined \u00B7 2 kontacts" />

                    <LinearLayout
                        android:id="@+id/yourGroupsRows"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:visibility="gone" />

                    <View
                        android:layout_width="match_parent"
                        android:layout_height="1dp"
                        android:background="@color/stats_card_border"
                        android:layout_marginTop="4dp"
                        android:layout_marginBottom="16dp" />

                    <!-- All Kontact Groups -->
                    <LinearLayout
                        android:id="@+id/allGroupsHeader"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center_vertical"
                        android:foreground="?attr/selectableItemBackground"
                        android:clickable="true"
                        android:focusable="true">

                        <ImageView
                            android:layout_width="14dp"
                            android:layout_height="14dp"
                            android:src="@drawable/ic_sync"
                            app:tint="@color/text_muted" />

                        <TextView
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_weight="1"
                            android:layout_marginStart="8dp"
                            android:text="ALL KONTACT GROUPS"
                            android:textSize="11sp"
                            android:textAllCaps="true"
                            android:letterSpacing="0.05"
                            android:textColor="@color/text_muted" />

                        <ImageView
                            android:id="@+id/allGroupsChevron"
                            android:layout_width="13dp"
                            android:layout_height="13dp"
                            android:src="@drawable/ic_chevron_down"
                            app:tint="@color/text_muted" />

                    </LinearLayout>

                    <TextView
                        android:id="@+id/allGroupsSubText"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:textSize="17sp"
                        android:textStyle="bold"
                        android:textColor="@color/vg_dark"
                        android:paddingTop="2dp"
                        android:paddingBottom="12dp"
                        tools:text="9 groups" />

                    <LinearLayout
                        android:id="@+id/allGroupsRows"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:visibility="gone" />

                </LinearLayout>

                <!-- Search-active view: one flat list, same divider-row
                     style as everything else. -->
                <LinearLayout
                    android:id="@+id/resultsView"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:visibility="gone">

                    <LinearLayout
                        android:id="@+id/resultsList"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical" />

                    <TextView
                        android:id="@+id/noResultsText"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:gravity="center"
                        android:textSize="12.5sp"
                        android:textColor="@color/text_muted"
                        android:paddingTop="20dp"
                        android:paddingBottom="20dp"
                        android:visibility="gone"
                        tools:text="No groups match \u201c7\u201d" />

                </LinearLayout>

                <TextView
                    android:id="@+id/allGroupsErrorText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Couldn\u2019t load groups. Check your connection and reopen this screen."
                    android:textSize="12.5sp"
                    android:textColor="@color/text_muted"
                    android:visibility="gone"
                    android:layout_marginTop="12dp" />

            </LinearLayout>

            <!-- Action buttons sit below the card, same pattern Profile
                 uses for its Contact Us button. -->
            <Button
                android:id="@+id/joinGroupsButton"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginTop="16dp"
                android:text="@string/btn_join_groups"
                android:textAllCaps="true"
                android:letterSpacing="0.02"
                android:backgroundTint="@color/vg_green"
                android:textColor="@color/white"
                android:textStyle="bold"
                app:icon="@drawable/ic_add"
                app:iconTint="@color/white"
                app:iconGravity="textStart"
                app:cornerRadius="16dp" />

            <Button
                android:id="@+id/noCodeContactUsButton"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginTop="12dp"
                android:text="@string/btn_no_code_contact_us"
                android:textAllCaps="true"
                android:letterSpacing="0.02"
                android:textStyle="bold"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                app:strokeColor="@color/primary_light"
                android:textColor="@color/primary_light"
                app:icon="@drawable/ic_chat"
                app:iconTint="@color/primary_light"
                app:iconGravity="textStart"
                app:cornerRadius="16dp" />

        </LinearLayout>

    </LinearLayout>

</androidx.core.widget.NestedScrollView>
