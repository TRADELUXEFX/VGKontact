<?xml version="1.0" encoding="utf-8"?>
<!--
    Collapsible header for a group section ("Your Groups" / "All Kontact
    Groups") inside the one-box Kontact Groups UI. Tapping toggles the
    matching body's visibility and rotates the chevron; wired in
    GroupsActivity. Matches .rect-section-header in
    kontact_groups_onebox.html.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="15dp"
    android:paddingBottom="15dp">

    <View
        android:id="@+id/sectionDot"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:layout_marginEnd="10dp"
        android:background="@drawable/group_check_mini_background"
        tools:ignore="MissingConstraints" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/sectionTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/vg_dark"
            android:fontFamily="@font/baloo2"
            tools:text="Your Groups" />

        <TextView
            android:id="@+id/sectionSub"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="1dp"
            android:textSize="11.5sp"
            android:textColor="@color/text_muted"
            tools:text="5 joined \u00B7 25 kontacts" />

    </LinearLayout>

    <ImageView
        android:id="@+id/sectionChevron"
        android:layout_width="13dp"
        android:layout_height="13dp"
        android:src="@drawable/ic_chevron_down"
        app:tint="@color/text_muted" />

</LinearLayout>
